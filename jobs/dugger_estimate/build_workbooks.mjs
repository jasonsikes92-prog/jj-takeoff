throw new Error(
  "Legacy Dugger workbook builder permanently disabled: it consumes the obsolete " +
  "hard-coded payload format. Create replacement workbooks only from a takeoff that " +
  "passes jnj_takeoff.certify_takeoff_for_pricing() after two independent measured " +
  "geometry proofs per core area component."
);
