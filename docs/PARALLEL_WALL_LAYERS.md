# Connected wall outlines beside unresolved layers

The Burns bathroom and laundry drawings contain close parallel outlines that initially withheld both alternatives. Source inspection shows that one outline in each group physically meets perpendicular wall faces at both end caps. The nearby inner outline stops short. Recovering the supported wall boundary separates the upper-left bedroom/bathroom and closes the laundry region.

The engine now tests those physical end-cap contacts within a parallel ambiguity group. It resolves an outline only when exactly one candidate has support at both ends from eligible perpendicular pieces outside all ambiguous groups. Edited members and explicit classifications prevent this automatic group resolution. The original CAD faces, company-depth compatibility, minimum network extent and anchors still apply. A numerical coordinate tolerance of 0.00001 PDF point only accommodates source arithmetic; nearby but disconnected geometry does not count as contact.

The competing outlines remain unresolved. This rule does not classify them as fixtures, erase them, approve lumber or establish wall assemblies. Competing-source hashes are included in the selected network evidence. Changing an unresolved outline can therefore invalidate the competing wall and its dependent openings, rooms and bid references. Explicit uncertain decisions retain precedence.

## Verified development results

The current Burns job retains reviewed sheet/view/style/scale inputs and company profile 13. No individual wall, opening or room-use answers were added. Of 97 wall candidates, 95 are inferred and two inner outlines remain unresolved. All 95 wall interpretations agree with the prior development source review; this is not independent accuracy evidence.

The derived result contains 16 candidate room regions, eleven inferred door roles (eight ordinary and three special), 13 inferred windows and nine unresolved opening roles. Four ordinary doors receive the saved passage-hardware function. Other served-room distinctions remain unresolved. Whole-house scope, purchase quantities and release are not certified.

108 focused network/opening tests and 79 related short-wall, room, quantity and LEVEL GROUND checks passed. A real HTTP trial changes an unresolved inner outline and confirms the competing wall inference and dependent door roles are withheld. Restoring geometry recovers the results. A separate current-source uncertain override also withdraws the inference and is preserved explicitly. The override is synthetic test evidence, not an owner decision. Original sources and Roberts live snapshot/workbook revision 22 remain unchanged.

Evidence: workspace `outputs/parallel_wall_layer_inference/checks.json`, `wall_runs.json`, `wall_runs_after_layer_edit.json`, `wall_runs_with_uncertain_override.json`, `opening_schedule.json`, `opening_schedule_after_layer_edit.json`, `opening_bid_draft.md`, and `estimate_draft.json`. Verifier: `work/phase3/verify_parallel_wall_layers.py`.

Source inspection crops are in `outputs/narrow_paired_door_inference/bed_bath_layers.png` and `laundry_wall_layers.png`. This development plan does not prove unfamiliar drafting-style coverage, independent takeoff accuracy, complete material quantities or current pricing.
